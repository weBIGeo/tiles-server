# Dependencies for the standalone util scripts (not needed by the server).
requests>=2.31
boto3>=1.34        # only for the S3 raw-GeoTIFF download
rasterio>=1.4      # CRS stamping in fetch_bev_als.py
# Optional: GDAL python bindings, only needed for `tif --crop-austria`.
# Install via your platform package manager / conda; pip install gdal is fiddly.
# gdal
